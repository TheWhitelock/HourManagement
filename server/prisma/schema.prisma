generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

enum ClockEventType {
  IN
  OUT
}

model ClockEvent {
  id         Int            @id @default(autoincrement())
  type       ClockEventType
  occurredAt DateTime
  createdAt  DateTime       @default(now())
}

