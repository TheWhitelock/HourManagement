generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Entry {
  id          Int      @id @default(autoincrement())
  description String
  hours       Float
  createdAt   DateTime @default(now())
}

